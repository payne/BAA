/* global $ */
/* global require */
$ = require('jquery');


var ArrayToTable = {
  insertInDom: function(csv_data, elementId) {
  //TODO: Use jQuery to create a table on elementId
  //hint -- you'll need two for loops 
  // -- one nested inside the other since csv_data is a two dimensional array.
};

module.exports=ArrayToTable; // TODO(ask): don't know why this is not working
window.ArrayToTable = ArrayToTable; // put function on the window, so it is global.
